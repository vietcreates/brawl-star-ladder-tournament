# Copy to ".env" and fill in your Supabase values.
# Without these, the app runs in local-only mode (single device).

VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-PUBLIC-KEY
